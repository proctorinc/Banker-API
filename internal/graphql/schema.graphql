type User {
    id: ID!
    username: String!
    email: String!
}

type Query {
    user(id: ID!): User
    users: [User!]
}

type Mutation {
    createUser(data: UserInput!): User!
    deleteUser(id: ID!): User!
}

input UserInput {
    username: String!
    email: String!
}
